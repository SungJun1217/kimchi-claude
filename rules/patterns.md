<!-- kimchi-ignore-file -->
# 문장 구조

단어가 아니라 문장 구조에서 나오는 번역 냄새다. 용어표로는 잡히지 않는 층이다.

`검사`가 `프롬프트`인 규칙은 문자열로 잡을 수 없어 출력 스타일만이 막을 수 있다.

| 원어 | 쓰지 말 것 | 쓸 것 | 이유 | 검사 | 순위 |
|---|---|---|---|---|---|
| — | ~되어질 | ~될 | 이중 피동 | 치환 | 핵심 |
| — | 보여집니다 | 보입니다 | 이중 피동 | 치환 | 핵심 |
| — | 사용되어지고 | 쓰이고 | 이중 피동 | 치환 | 핵심 |
| your | 당신의 | 이 / 생략 | 영어 your를 직역했다. 한국어에서는 대개 생략한다 | 정규식 | 핵심 |
| we | 우리는 ~합니다 | 주어를 빼고 대상을 주어로 놓습니다 | 영어의 인칭 주어를 직역했다. "우리는 캐시를 재생성한다"가 아니라 "캐시가 매번 새로 만들어집니다" | 프롬프트 | 핵심 |
| — | ~를 수행합니다 | ~합니다 | 기능동사 남용. "검사를 수행합니다"는 "검사합니다". 단 앞말이 동사로 못 쓰는 명사면("배치를 수행합니다", "스크립트를 수행합니다") "를 수행합니다"만 지워도 "배치합니다"·"스크립트합니다"처럼 없는 말이 되거나 뜻이 바뀐다. 앞말을 봐야 하는 교정이라 자동으로 하지 않는다 | 정규식 | 핵심 |
| — | ~에 대한 처리를 진행 | ~를 처리 | 영어 명사구를 직역한 뒤 기능동사를 붙였다 | 정규식 | 핵심 |
| We regenerate the cache on every call. | 우리는 호출마다 캐시를 재생성합니다. | 호출마다 캐시가 새로 만들어집니다. | 영어 we 직역. 한국어 기술 설명문은 주어를 비웁니다. | 치환 | 핵심 |
| You can run the tests with npm test. | 당신은 npm test로 테스트를 실행할 수 있습니다. | npm test로 테스트를 돌릴 수 있습니다. | you를 '당신'으로 옮기면 번역서 문체가 됩니다. | 정규식 | 핵심 |
| Your code has a race condition. | 당신의 코드에 race condition이 있습니다. | 이 코드에 race condition이 있습니다. | 소유격 your는 '이'로 바꾸거나 빼는 쪽이 자연스럽습니다. | 치환 | 핵심 |
| handle the request | 요청에 대한 처리를 진행합니다. | 요청을 처리합니다. | 명사구+기능동사 직역. 동사 하나로 끝납니다. | 치환 | 핵심 |
| perform validation on the input | 입력값에 대한 검증을 수행합니다. | 입력값을 검증합니다. | '수행하다'는 대개 지울 수 있는 군더더기입니다. | 치환 | 핵심 |
| This value will be removed in the next release. | 이 값은 다음 배포에서 제거되어질 예정입니다. | 이 값은 다음 배포에서 제거됩니다. | '되어질'은 이중 피동입니다. 한국어에 없는 형태입니다. | 정규식 | 핵심 |
| This looks like a caching problem. | 캐시 문제로 보여집니다. | 캐시 문제로 보입니다. | '보여지다'는 이중 피동입니다. 국립국어원 온라인가나다 310695 | 치환 | 핵심 |
| This helper is only used in tests. | 이 헬퍼는 테스트에서만 사용되어집니다. | 이 헬퍼는 테스트에서만 사용됩니다. | 피동은 한 겹으로 충분합니다. | 치환 | 핵심 |
| The value is stored by the cache. | 이 값은 캐시에 의해 저장됩니다. | 이 값은 캐시에 저장됩니다. | 영어 by를 '에 의해'로 옮기면 어색합니다. 조사로 해결됩니다. 공공언어 바로 쓰기(2022 개정), 새국어생활 2012 | 치환 | 핵심 |
| We modified several files. | 여러 파일들을 수정했습니다. | 여러 파일을 수정했습니다. | '여러'가 이미 복수입니다. '들'이 겹칩니다. 새국어생활 2012 p.76, MS 한국어 스타일 가이드 4.1.8 | 치환 | 핵심 |
| This error tells you that the config is missing. | 이 오류는 당신에게 설정이 빠졌다고 알려줍니다. | 이 오류는 설정이 빠졌다는 뜻입니다. | 무생물이 사람에게 말하는 구조는 영어 문법입니다. | 정규식 | 핵심 |
| This test shows us that the cache is empty. | 이 테스트는 우리에게 캐시가 비어 있음을 보여줍니다. | 이 테스트를 보면 캐시가 비어 있습니다. | '~는 우리에게 ~을 보여준다'는 통째로 영어 구문입니다. | 정규식 | 핵심 |
| This change brings a performance improvement. | 이 변경은 성능 향상을 가져옵니다. | 이렇게 바꾸면 빨라집니다. | 무생물 주어가 무엇을 '가져오는' 구조는 영어식입니다. | 프롬프트 | 핵심 |
| That is because the cache is cleared on every request. | 그것은 캐시가 요청마다 비워지기 때문입니다. | 캐시가 요청마다 비워지기 때문입니다. | That is because의 that은 옮기지 않습니다. | 치환 | 핵심 |
| It is important to release the lock. | 락을 해제하는 것은 중요합니다. | 락은 반드시 해제해야 합니다. | It is important to 직역. 한국어는 당위 표현으로 바로 씁니다. 다만 "중요합니다"는 우선순위를, "해야 합니다"는 의무를 말해 뜻이 달라집니다. 정말 중요도만 말하려는 문장(예: 여러 일 중 이 일이 더 중요하다는 비교)까지 의무 표현으로 바꾸면 안 됩니다. | 정규식 | 핵심 |
| It's worth noting that this function is idempotent. | 주목할 점은 이 함수가 멱등이라는 것입니다. | 이 함수는 멱등입니다. | 영어 강조 구문을 옮기면 빈 절이 하나 늘어납니다. | 치환 | 핵심 |
| This means the request is sent twice. | 이것은 요청이 두 번 전송된다는 것을 의미합니다. | 그래서 요청이 두 번 전송됩니다. | This means 직역. 접속사 하나로 끝납니다. | 정규식 | 핵심 |
| If the test fails, then check the log. | 만약 테스트가 실패한다면, 그러면 로그를 확인하세요. | 테스트가 실패하면 로그를 보세요. | if~then의 then은 한국어 어미에 이미 들어 있습니다. | 정규식 | 핵심 |
| I fixed the function that was causing slow responses because it rebuilt the cache on every call. | 호출마다 캐시를 새로 만들어서 응답을 느리게 만들고 있던 이 함수를 수정했습니다. | 이 함수가 호출마다 캐시를 새로 만들고 있었습니다. 그래서 응답이 느렸고, 지금은 고쳤습니다. | 영어 관계절을 명사 앞에 쌓지 말고 문장을 끊습니다. | 프롬프트 | 핵심 |
| This change removes the cache rebuild, which reduces latency, while all tests still pass and backward compatibility is preserved. | 이 변경은 캐시 재생성을 없애 latency를 줄이면서, 동시에 모든 테스트를 통과하고 하위 호환도 유지합니다. | 캐시 재생성을 없애 latency를 줄였습니다. 테스트는 전부 통과하고 하위 호환도 그대로입니다. | 영어 한 문장을 한국어 한 문장으로 옮기면 늘어집니다. 끊어 씁니다. latency는 실무에서 원어를 그대로 씁니다. | 프롬프트 | 핵심 |
| This class has three dependencies. | 이 클래스는 세 개의 의존성을 가지고 있습니다. | 이 클래스는 의존성이 세 개입니다. | have 직역. 한국어는 '있다'로 씁니다. 대체 표현은 문맥마다 달라('없다', '맺다' 등) 정규식으로만 안내합니다. MS 한국어 스타일 가이드 4.1.11, 새국어생활 2012 | 정규식 | 핵심 |
| This flag allows you to skip the cache. | 이 플래그는 당신이 캐시를 건너뛸 수 있도록 허용합니다. | 이 플래그를 주면 캐시를 건너뜁니다. | allow you to 직역은 주어와 목적어를 둘 다 늘립니다. | 정규식 | 핵심 |
| — | 한 문장에 절을 셋 이상 이어 붙이기 | 절 두 개까지. 50자를 넘으면 문장을 나눕니다 | 영어 한 문장을 한국어 한 문장으로 고집하면 늘어집니다 | 프롬프트 | 핵심 |
| — | 파일들을 | 파일을 | 복수 접미사 남용이지만, 수량 표현이 없으면 '들'이 종류의 다양성을 뜻할 수 있어 문맥을 봐야 한다("파일들을 뒤졌다"는 여러 종류를 뒤졌다는 뜻도 된다). 위 핵심 행들은 수량 표현이 있는 문장 전체를 고정해 둔 것이라 안전한 치환이고, 이 낱말 자체를 일반화한 규칙은 아니다. 새국어생활 2012 p.76 | 정규식 | 보통 |
| — | ~잊혀지다 | ~잊히다 | 이중 피동. 피동 접사 '히'에 '-어지다'를 다시 붙였다. 국립국어원 온라인가나다 310695 | 치환 | 보통 |
| — | ~잊혀졌다 | ~잊혔다 | 이중 피동의 과거형 | 치환 | 보통 |
| — | ~잊혀진 | ~잊힌 | 이중 피동의 관형사형(과거) | 치환 | 보통 |
| — | ~잊혀지는 | ~잊히는 | 이중 피동의 관형사형(현재) | 치환 | 보통 |
| — | ~잊혀질 | ~잊힐 | 이중 피동의 관형사형(미래) | 치환 | 보통 |
| — | ~되어지다 | ~되다 | 이중 피동. 되다는 그 자체로 피동의 뜻을 담아 '-어지다'를 다시 붙일 필요가 없다. 국립국어원 온라인가나다 310695 | 치환 | 보통 |
| — | ~되어졌다 | ~됐다 | 이중 피동의 과거형 | 치환 | 보통 |
| — | ~되어진 | ~된 | 이중 피동의 관형사형(과거) | 치환 | 보통 |
| — | ~되어지는 | ~되는 | 이중 피동의 관형사형(현재) | 치환 | 보통 |
| (person) from | ~로부터 받았다 | ~에게서 받았다 (사물·기관이면 ~에서 받았다) | 영어 from + 사람 전치사구 직역이다. 사람·동물이면 '~에게서'가 맞지만, 이 낱말 자체는 서버·API·캐시·환경 변수·업스트림처럼 사물·기관에도 흔히 붙어('업스트림으로부터 받았다') 그때는 '~에서'가 맞다. 문맥마다 갈려 정규식으로만 안내한다. 새국어생활 2012 3.3절 | 정규식 | 보통 |
| (person) from | ~로부터 들었다 | ~에게서 들었다 (사물·기관이면 ~에서 들었다) | 위와 같다 | 정규식 | 보통 |
| by (수동태 행위자, 결정) | ~에 의해 결정 | ~가 결정 / ~에서 결정 (도구·설정이면 ~로 결정) | 영어 by 수동태 또는 일본어 번역 투다. 행위자가 사람·기관이면 능동 '~가 결정'이, 장소면 '~에서 결정'이, 도구·기준이면 '~로 결정'이 맞아 문맥마다 갈린다('위원회에 의해 결정됩니다'를 '위원회로 결정됩니다'로 바꾸면 위원회가 선정된 대상으로 읽혀 뜻이 달라진다. '법에 의하여'처럼 근거를 뜻하는 정당한 쓰임도 있다). 정규식으로만 안내한다. 공공언어 바로 쓰기(2022 개정), 새국어생활 2012 | 정규식 | 보통 |
| by (수동태 행위자, 결정) | ~에 의하여 결정 | ~가 결정 / ~에서 결정 (도구·설정이면 ~로 결정) | 위와 같다 | 정규식 | 보통 |
| it is important to | ~하는 것은 중요합니다 | ~해야 합니다 | 영어 가주어 구문을 직역했다 | 프롬프트 | 보통 |
| note that | 주목할 점은 ~라는 것입니다 | ~입니다 | 영어 강조 구문을 직역했다 | 프롬프트 | 보통 |
| — | 그것은 ~하기 때문입니다 | ~때문입니다 | 영어 대명사 주어를 직역했다 | 정규식 | 보통 |
| — | 한 문장을 원문 길이 그대로 옮기기 | 두세 문장으로 끊어 쓰기 | 영어 한 문장을 한국어 한 문장으로 고집하면 늘어진다 | 프롬프트 | 보통 |
| Let's check the logs first. | 우리가 먼저 로그를 확인해 봐야 합니다. | 먼저 로그를 확인해 봐야 합니다. | we를 주어로 살리면 내 일이 아닌 것처럼 들립니다. | 치환 | 보통 |
| I have fixed the bug. | 저는 이 버그를 수정했습니다. | 이 버그를 수정했습니다. | 1인칭 주어는 한국어에서 기본적으로 생략합니다. | 치환 | 보통 |
| your project's configuration file | 당신의 프로젝트의 설정 파일을 수정했습니다. | 프로젝트 설정 파일을 수정했습니다. | '의'가 겹치면 읽기 어려워집니다. 관형격은 한 번만 씁니다. | 프롬프트 | 보통 |
| To answer your question, ... | 당신의 질문에 답하자면, 이 값은 요청마다 새로 계산됩니다. | 이 값은 요청마다 새로 계산됩니다. | 영어식 답변 서두는 한국어에서 통째로 뺍니다. | 정규식 | 보통 |
| It is recommended that you call init() first. | init()을 먼저 호출하는 것이 권장됩니다. | init()을 먼저 호출해야 합니다. | It is recommended 직역. 한국어는 당위 표현으로 바로 말합니다. | 정규식 | 보통 |
| We performed a refactoring of the module. | 모듈에 대한 리팩터링 작업을 실시했습니다. | 모듈을 리팩터링했습니다. | '작업을 실시'는 문장만 길게 만듭니다. | 치환 | 보통 |
| Performance needs to be improved. | 성능 개선을 위한 작업이 필요한 상황입니다. | 성능을 개선해야 합니다. | 동사를 명사로 만든 뒤 '작업', '상황'을 붙이면 뜻이 흐려집니다. | 프롬프트 | 보통 |
| We verified that it is reproducible. | 재현이 가능한지에 대한 확인을 했습니다. | 재현되는지 확인했습니다. | '~에 대한 확인을 하다'는 '확인하다'로 줄어듭니다. | 치환 | 보통 |
| Tests are run by CI. | 테스트는 CI에 의해 실행됩니다. | 테스트는 CI에서 돌아갑니다. | 행위자 표시는 '에서'로 충분합니다. 공공언어 바로 쓰기(2022 개정) | 치환 | 보통 |
| This config is not used anywhere. | 이 설정은 어디에서도 쓰여지지 않습니다. | 이 설정은 어디에서도 쓰이지 않습니다. | '쓰여지다'는 이중 피동입니다. 다만 '쓰여지다'는 문맥에 따라 '쓰이다'와 '써지다' 둘 다로 고칠 수 있어, 이 행은 문장 전체를 고정해 둔 것이지 낱말만 떼어 낸 일반 규칙이 아니다. 국립국어원 온라인가나다 310695 | 치환 | 보통 |
| Many tests failed. | 많은 테스트들이 실패했습니다. | 테스트 여러 개가 실패했습니다. | 수량 표현 뒤의 '들'은 군더더기입니다. 새국어생활 2012 p.76 | 치환 | 보통 |
| We updated all dependencies. | 모든 의존성들을 최신으로 올렸습니다. | 의존성을 전부 최신으로 올렸습니다. | '모든 ~들'은 복수 표시가 두 번 들어갑니다. 새국어생활 2012 p.76 | 치환 | 보통 |
| This document explains the setup. | 이 문서는 설치 방법을 설명해 줍니다. | 설치 방법은 이 문서에 있습니다. | 사물이 '설명해 주는' 구조는 한국어에서 겉돕니다. | 프롬프트 | 보통 |
| This patch will help resolve the issue. | 이 패치는 문제를 해결하는 데 도움을 줄 것입니다. | 이 패치로 문제가 해결됩니다. | help 직역이 문장을 두 배로 늘립니다. | 정규식 | 보통 |
| Interestingly, the retry happens twice. | 재시도가 두 번 일어난다는 점이 흥미롭습니다. | 재시도가 두 번 일어납니다. | 가주어 감상 표현 대신 사실과 판단을 나눠 씁니다. | 정규식 | 보통 |
| in the case of a timeout | 타임아웃인 경우에 있어서는 재시도를 합니다. | 타임아웃이면 재시도합니다. | '경우에 있어서'는 어미 하나로 줄어듭니다. | 치환 | 보통 |
| For that reason, we rolled back. | 그러한 이유로 인하여 배포를 되돌렸습니다. | 그래서 배포를 되돌렸습니다. | '~로 인하여'는 대개 '그래서'로 끝납니다. | 치환 | 보통 |
| in terms of performance | 성능의 관점에서 보았을 때 이 방식이 더 낫습니다. | 성능만 보면 이 방식이 낫습니다. | in terms of 직역이 수식절을 늘립니다. | 정규식 | 보통 |
| with respect to memory usage | 메모리 사용량에 있어서는 차이가 없습니다. | 메모리 사용량은 차이가 없습니다. | '~에 있어서'는 조사 '은/는'으로 충분합니다. | 치환 | 보통 |
| things like retries and timeouts | 재시도나 타임아웃과 같은 것들을 먼저 확인했습니다. | 재시도와 타임아웃부터 확인했습니다. | '~와 같은 것들'은 항목을 그대로 나열하면 사라집니다. | 정규식 | 보통 |
| This function has been working this way for a long time. | 이 함수는 오랫동안 이렇게 동작해 왔습니다. | 이 함수는 예전부터 이렇게 동작합니다. | 현재완료를 '~해 왔습니다'로 옮기면 늘어집니다. | 치환 | 보통 |
| I will clean this up in the next commit. | 다음 커밋에서 이 부분을 정리할 것입니다. | 다음 커밋에서 이 부분을 정리하겠습니다. | will 직역보다 '-겠습니다'가 자연스럽습니다. | 프롬프트 | 보통 |
| Also, I added logs. Also, I strengthened the tests. | 또한 로그를 추가했습니다. 또한 테스트도 보강했습니다. | 로그를 추가하고 테스트도 보강했습니다. | Also를 문장마다 옮기면 읽는 리듬이 끊깁니다. | 프롬프트 | 보통 |
| Therefore we disabled the cache. Therefore responses got faster. | 따라서 캐시를 껐습니다. 따라서 응답이 빨라졌습니다. | 캐시를 껐더니 응답이 빨라졌습니다. | 인과는 접속 부사보다 어미로 붙이는 쪽이 짧습니다. | 프롬프트 | 보통 |
| This structure is extensible and flexible. | 이 구조는 확장적이고 유연적입니다. | 이 구조는 확장성이 좋고 유연합니다. | '-적'을 아무 데나 붙이면 없는 말이 됩니다. | 치환 | 보통 |
| in terms of readability | 가독성적인 측면에서 개선이 필요합니다. | 가독성을 개선해야 합니다. | '-적인 측면에서'는 빼도 뜻이 그대로 남습니다. | 치환 | 보통 |
| This improves stability, scalability, and maintainability. | 이 방식은 안정성과 확장성과 유지보수성을 모두 향상시킵니다. | 이 방식은 안정성, 확장성, 유지보수성이 모두 좋아집니다. | '-성' 명사를 나열하면 무슨 말인지 흐려집니다. | 프롬프트 | 보통 |
| We externalized the configuration. | 설정을 외부화하는 작업을 진행했습니다. | 설정을 별도 파일로 분리했습니다. | '-화' 뒤에 '작업을 진행'까지 붙으면 두 겹으로 늘어납니다. | 치환 | 보통 |
| This is a race condition. | 이것은 하나의 경쟁 조건입니다. | 경쟁 조건입니다. | 영어 부정관사는 옮기지 않습니다. | 치환 | 보통 |
| This is one of the most common mistakes. | 이것은 가장 흔한 실수들 중의 하나입니다. | 흔한 실수입니다. | one of the 직역은 길기만 합니다. 다만 그대로 꽂으면 "가장"(제일 흔하다는 최상급)이 사라져 그냥 "흔한" 정도로 뜻이 약해집니다. | 정규식 | 보통 |
| You need to adjust the retry count. | 재시도 횟수를 조정할 필요가 있습니다. | 재시도 횟수를 조정해야 합니다. | need to 직역. '-해야 합니다'가 짧습니다. | 치환 | 보통 |
| This value ends up being computed twice. | 이 값이 두 번 계산되게 됩니다. | 이 값이 두 번 계산됩니다. | '되게 됩니다'는 피동이 겹친 형태입니다. | 치환 | 보통 |
| This setting disables the cache. | 이 설정이 캐시를 비활성화시켜 줍니다. | 이 설정은 캐시를 비활성화합니다. | '-화시키다'는 '-하다'로 충분합니다. '-해 주다'도 군더더기입니다. | 치환 | 보통 |
| Don't forget to run the migration. | 마이그레이션을 실행하는 것을 잊지 마세요. | 마이그레이션도 실행해야 합니다. | Don't forget to 직역보다 할 일을 바로 말합니다. 다만 그대로 꽂으면 원문에 없던 "도"(다른 일과 함께)가 끼어들고, 부탁("~하세요")이 진술("~해야 합니다")로 바뀌어 문장의 성격이 달라집니다. | 정규식 | 보통 |
| If you want, you can turn this check off. | 만약 당신이 원한다면 이 검사를 끌 수 있습니다. | 필요하면 이 검사를 끌 수 있습니다. | if you want는 '필요하면'으로 줄입니다. | 정규식 | 보통 |
| Let's talk about the caching strategy. | 이제 캐시 전략에 대해 이야기해 봅시다. | 캐시 전략을 보겠습니다. | 영어식 진행 안내문은 한국어에서 대개 뺍니다. | 치환 | 보통 |
| I think this part is the cause. | 저는 이 부분이 원인이라고 생각합니다. | 이 부분이 원인으로 보입니다. | I think 직역보다 추정 어미로 처리합니다. 다만 "생각합니다"(주관적 의견)와 "보입니다"(근거에 따른 추정)는 화자가 자기 판단에 싣는 확신의 종류가 달라, 정말 개인 의견임을 밝히려는 문장에서는 자동으로 바꾸지 않습니다. | 정규식 | 보통 |
| There is a bug in this handler. | 이 핸들러에는 하나의 버그가 존재합니다. | 이 핸들러에 버그가 있습니다. | there is 직역에 '존재'까지 붙으면 무거워집니다. | 치환 | 보통 |
| This makes the code more readable. | 이것은 코드를 더 읽기 쉽게 만들어 줍니다. | 이렇게 하면 코드가 읽기 쉬워집니다. | make 직역 '만들어 주다'는 대개 '-어집니다'로 바뀝니다. | 치환 | 보통 |
| if ... then | 만약 ~라면, 그러면 | ~면 | 조건문을 두 겹으로 직역했다 | 정규식 | 참고 |
| This function is called twice. | 이 함수가 두 번 불려집니다. | 이 함수가 두 번 호출됩니다. | 이중 피동이고, 기술 문맥에서는 '호출'이 정착어입니다. | 치환 | 참고 |
| I had already run the tests. | 테스트는 이미 실행했었습니다. | 테스트는 이미 실행했습니다. | 과거완료 직역. 시점은 '이미'가 이미 표시합니다. | 치환 | 참고 |
| I modified three files. | 3개의 파일을 수정했습니다. | 파일 3개를 수정했습니다. | 영어 수량 표현 순서를 그대로 두면 어색합니다. 숫자와 명사의 자리를 통째로 맞바꿔 쓸 것이 같은 자리 숫자로 시작하지 않으므로("50 %"→"50%"와 다르다) 자동 치환은 하지 않는다 | 정규식 | 참고 |
| As mentioned above, this path is unused. | 위에서 언급된 바와 같이 이 경로는 사용되지 않습니다. | 앞에서 말했듯이 이 경로는 쓰이지 않습니다. | 영어 수동 인용 구문을 능동으로 바꿉니다. | 정규식 | 참고 |
| It is not that this approach doesn't work. | 이 방식이 동작하지 않는 것은 아닙니다. | 이 방식도 동작하기는 합니다. | 이중 부정을 그대로 옮기면 뜻이 늦게 도착합니다. | 프롬프트 | 참고 |
| The tests are being run. | 테스트가 실행되고 있는 중입니다. | 테스트가 돌고 있습니다. | 진행 표현에 '중'까지 겹칩니다. | 치환 | 참고 |
| And I also made it write logs. | 그리고 로그도 남기도록 했습니다. | 로그도 남기도록 했습니다. | And로 문장을 시작하는 습관은 한국어에서 더 튑니다. | 프롬프트 | 참고 |
| — | 수정 완료. 확인 필요. | 수정했습니다. 확인이 필요합니다. | 명사로 끊은 전보체다. 조사와 어미를 살려 서술어로 끝맺는다. 제목과 짧은 목록 항목은 예외다. 출처: fluent-korean, 실험에서 두 평가자가 모두 감점했다 | 프롬프트 | 핵심 |
| — | 한 가지 알려드립니다: 할인 금액을 반올림하지 않아서 | 한 가지 더 있습니다. 할인 금액을 반올림하지 않아서 | 문장 가운데를 쌍점이나 엠대시로 잇는 것은 영어 구두점 습관이다. 접속사를 쓰거나 두 문장으로 나눈다. 목록 머리의 쌍점은 괜찮다 | 프롬프트 | 보통 |
| it will be | 예정에 있습니다 | 예정입니다 | '~에 있다'는 예정 뒤에 붙는 일본어 번역 투다. 예정은 늘 동사 관형형 뒤에만 와서 자동 치환해도 안전하다. 공공언어 바로 쓰기 p.13 | 치환 | 참고 |
| attention required | 주의가 요구됩니다 | 주의해야 합니다 | 불필요한 피동 -되다. '각별한'처럼 주의를 꾸미는 말이 앞에 오면 그대로 바꿨을 때 문장이 깨져('각별한 주의해야 합니다') 자동 치환은 하지 않는다. 공공언어 바로 쓰기 p.52 | 정규식 | 참고 |
| in (Japanese calque) | ~에 있어서 | ~에서 / ~에 대해 / ~의 | 일본어 번역 투다. 대체할 조사가 문맥마다 달라 자동 치환은 못 한다. 다만 "서버에 있어서는 안 됩니다", "있어서 다행입니다"처럼 있다(존재)의 활용형과 문자열이 같아 정규식으로는 둘을 가르지 못한다. 문자열로 잡을 수 없는 문맥 판단이라 스타일로만 안내한다. | 프롬프트 | 보통 |
| in (일본어 번역 투 계사) | 이 기능의 목표는 응답 속도를 높이는 데에 있다. | 이 기능의 목표는 응답 속도를 높이는 것이다. | '~에 있다'를 서술격으로 쓰는 일본어 번역 투다. 장소를 뜻하는 진짜 '~에 있다'(존재)와 겹친다 — "원인은 부주의에 있다"는 그대로도 자연스럽게 읽혀 문자열로는 둘을 못 가른다. 공공언어 바로 쓰기, '일본어 번역 투 삼가기' | 프롬프트 | 참고 |
| need | ~필요로 하 / 필요로 합니다 | ~이 필요하 | 영어 need 직역이다. 목적격 '을/를'을 주격 '이/가'로 바꿔야 하는데 받침에 따라 갈려 자동 치환은 못 한다. 공공언어 바로 쓰기 p.88 | 정규식 | 보통 |
| causative | 실행시킵니다 | 실행합니다 | 불필요한 사동 -시키다. 다른 대상에게 정말 시키는 진짜 사동 문맥과 문자열만으로 구분할 수 없어 자동 치환은 하지 않는다. 공공언어 바로 쓰기 p.56 | 정규식 | 보통 |
| causative | 변경시킵니다 | 변경합니다 | 불필요한 사동 -시키다. 다른 대상에게 정말 시키는 진짜 사동 문맥과 문자열만으로 구분할 수 없어 자동 치환은 하지 않는다. 공공언어 바로 쓰기 p.56 | 정규식 | 보통 |
| causative | 완료시킵니다 | 완료합니다 | 불필요한 사동 -시키다. 다른 대상에게 정말 시키는 진짜 사동 문맥과 문자열만으로 구분할 수 없어 자동 치환은 하지 않는다. 공공언어 바로 쓰기 p.56 | 정규식 | 보통 |
| located in | ~에 위치한 | ~에 있는 | 영어 located in 직역이다. 공공언어 바로 쓰기는 '~에 있는'을 권한다. 공공언어 바로 쓰기 p.54 | 정규식 | 참고 |
| through | 이 옵션을 통해 검증을 건너뛸 수 있습니다. | 이 옵션으로 검증을 건너뛸 수 있습니다. | '통해'는 API를 통해 호출처럼 실제 경로를 가리킬 때는 자연스럽다. 수단을 나타낼 때는 '으로'가 더 자연스럽다. 공공언어 바로 쓰기 p.20 | 프롬프트 | 참고 |
| regarding | 이 절은 설정에 대한 정보를 설명합니다. | 이 절은 설정 정보를 설명합니다. | 영어 about/regarding 직역이다. 개발 문서에 너무 흔해 문자열로 잡으면 오탐이 쏟아져 스타일로만 안내한다. 공공언어 바로 쓰기 p.64, p.42 | 프롬프트 | 참고 |
